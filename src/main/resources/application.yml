spring:
  datasource:
    url: jdbc:postgresql://localhost/angualr
    username: postgres
    password: Admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect